# Delve

Delve is a Go debugger. It's great if you remain in Go code as it understands the Go runtime better than the likes of gdb. However, if you start using cgo and need to cross into C functions delve is pretty useless with gdb being a superior option.
